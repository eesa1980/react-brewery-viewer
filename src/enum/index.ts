export enum Direction {
  ASC = "asc",
  DESC = "desc",
}

export enum SortBy {
  NAME = "name",
  STATE = "state",
  TYPE = "type",
}

export enum BreweryType {
  ALL = "all",
  BAR = "bar",
  BREWPUB = "brewpub",
  CLOSED = "closed",
  CONTRACT = "contract",
  LARGE = "large",
  MICRO = "micro",
  NANO = "nano",
  PLANNING = "planning",
  PROPRIETOR = "proprietor",
  REGIONAL = "regional",
}
